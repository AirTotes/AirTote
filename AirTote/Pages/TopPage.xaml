<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
	xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
	xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
	xmlns:maps="clr-namespace:AirTote.Components.Maps"
	x:Class="AirTote.Pages.TopPage"
	Title="Flight Information">
	<ContentPage.Resources>
		<x:Double x:Key="BtnWidth">48</x:Double>

		<Style
			TargetType="Button"
			x:Key="MenuButton">
			<Setter
				Property="Margin"
				Value="4"/>
			<Setter
				Property="Padding"
				Value="0"/>
			<Setter
				Property="TextColor"
				Value="{AppThemeBinding Light={StaticResource LightSecondaryColor}, Dark={StaticResource DarkSecondaryColor}}" />
			<Setter
				Property="BackgroundColor"
				Value="Transparent" />
			<Setter
				Property="FontSize"
				Value="36"/>
		</Style>
	</ContentPage.Resources>

	<AbsoluteLayout
		IgnoreSafeArea="True">
		<maps:AirportMap
			AbsoluteLayout.LayoutBounds="0,0,1,1"
			AbsoluteLayout.LayoutFlags="All"
			x:Name="Map"/>

		<Rectangle
			Style="{StaticResource LightDarkBGRect}"
			AbsoluteLayout.LayoutBounds="0,50,48,96"
			Aspect="Fill"/>
		<Button
			AbsoluteLayout.LayoutBounds="0,50,48,48"
			Style="{StaticResource MenuButton}"
			Text="&#x2261;"
			Clicked="MenuButton_Clicked"/>
	</AbsoluteLayout>
</ContentPage>